import React from "react";
import { View } from "react-native";

// Composant temporaire pour éviter les erreurs d'import
// À implémenter plus tard selon les besoins

export const CameraWithFilters = () => {
  return <View />;
};
