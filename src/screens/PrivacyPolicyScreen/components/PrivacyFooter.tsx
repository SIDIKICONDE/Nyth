import * as React from "react";
import { PrivacyFooterProps } from "../types";

export const PrivacyFooter = ({
  hasScrolledToBottom,
  onAccept,
  onDecline,
  currentTheme,
}: PrivacyFooterProps) => {
  // Plus de footer nécessaire - retourner null pour ne rien afficher
  return null;
};
